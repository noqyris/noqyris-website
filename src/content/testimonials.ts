import type { Testimonial } from "./types";

// Empty in v1 — the Proof section renders an honest empty state until real,
// named testimonials exist. Never ship invented quotes.
export const testimonials: Testimonial[] = [];
